[project]
name = "kr-prox-grad"
version = "2026.0.0"
requires-python = ">=3.14"
authors = [{ name = "Arsen Hnatiuk", email = "arsen.hnatiuk@hu-berlin.de" }]
readme = "README.md"
dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = { file = ["environment.yml"] }
